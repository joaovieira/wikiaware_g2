page.replace_html "wikiaware_section_rating_#{@section_id}", :partial => 'ratings/show_section', :locals => { :section_id => @section_id, :version_id => @version_id, :page_id => @page_id, :user_id => @user_id }

#page.visual_effect :highlight, :like_ratings, :duration => 1

#page.replace_html :menu_message, 'You liked this page'
#page.visual_effect :highlight, :menu_message
