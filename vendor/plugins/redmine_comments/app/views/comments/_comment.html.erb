<div class="comment">
	<%= link_to User.find(comment.user_id).login, '#' %> #user_path(@user)
	comentou (<%= time_ago_in_words(comment.created_at) %> ago): <br/>
	<%= comment.content %><br/>
	<br/>
</div>
